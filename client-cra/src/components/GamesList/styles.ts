import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    table: {
        margin: 'auto'
    },
    header: {
        padding: 10,
        textAlign: 'left',
    }
});

export default useStyles